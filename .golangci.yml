run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      check-blank: true
    staticcheck:
      checks: ["all"]

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters: [errcheck]
