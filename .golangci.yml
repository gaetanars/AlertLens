version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      check-blank: true
    staticcheck:
      checks: ["all"]
  exclusions:
    rules:
      - path: "_test\\.go"
        linters: [errcheck]
